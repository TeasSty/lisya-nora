/**
 * Текущий этап проекта: заказчица сначала смотрит только дизайн и вёрстку.
 * Пока это так, каталог отдаёт статичные демо-данные, а форма заявки работает
 * как полноценный UI (валидация, состояние отправки, успех) без реального
 * сохранения на сервере — backend и панель администратора уже готовы в коде
 * (см. worker/ и src/pages/admin), но подключение отложено до следующего этапа.
 *
 * Когда дизайн одобрят и Cloudflare Worker будет развёрнут — поменяйте
 * DEMO_MODE на false. Остальной код (каталог, форма заявки) менять не нужно:
 * он уже написан так, чтобы одинаково работать в обоих режимах.
 */
export const DEMO_MODE = true
